export default function PlaceholderChart() {
  return (
    <div style={{ padding: 12, border: "1px dashed #ccc", borderRadius: 8 }}>
      Chart placeholder (Recharts wrapper will live here).
    </div>
  );
}

